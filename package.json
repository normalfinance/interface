{
  "name": "@normalfinance/normal-v1-stellar-frontend",
  "description": "The Phoenix Frontend is a TypeScript-based web application utilizing yarn workspaces. It includes a UI kit (MUI-based), state management, utility functions, and a core package for seamless integration. It's written to use it with phoenix-contracts on Stellar's soroban platform.",
  "version": "0.0.1",
  "scripts": {
    "test": "yarn workspace @normalfinance/utils run test --passWithNoTests",
    "build": "yarn workspace @normalfinance/utils run build && yarn workspace @normalfinance/contracts run build && yarn workspace @normalfinance/core run build",
    "dev": "yarn workspace @normalfinance/web dev",
    "build:web": "yarn workspace @normalfinance/web build",
    "build:contracts": "yarn workspace @normalfinance/contracts build",
    "build:utils": "yarn workspace @normalfinance/utils build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/normalfinance/normal-v1-stellar-frontend.git"
  },
  "author": "Normal",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/normalfinance/normal-v1-stellar-frontend/issues"
  },
  "homepage": "https://github.com/normalfinance/normal-v1-stellar-frontend",
  "workspaces": [
    "packages/contracts",
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0"
  },
  "packageManager": "yarn@3.6.4",
  "dependencies": {
    "@stellar/stellar-sdk": "^12.1.0",
    "@walletconnect/modal": "^2.6.2",
    "@walletconnect/sign-client": "^2.13.1",
    "@walletconnect/types": "^2.13.1",
    "next": "15.0.3"
  }
}
