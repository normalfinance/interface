// ----------------------------------------------------------------------

interface Props {
  title: React.ReactNode;
  index: any; //Index;
}

const PositionTab = ({ title, index, ...other }: Props) => {
  return <></>;
};

export { PositionTab };
